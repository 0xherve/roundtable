/** @type {import('next').NextConfig} */
const nextConfig = {
  // For static export (if needed)
  // output: 'export',
  // trailingSlash: true,
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig